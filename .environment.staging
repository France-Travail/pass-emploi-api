# Profil STAGING : DB staging via tunnel Scalingo + keycloak staging. Utilisé par `yarn start:staging` / `yarn watch:staging`.
# Chargé AVANT .environment (premier fichier gagne) ; les ${…} sont résolus depuis le vault.
# Le tunnel est ouvert automatiquement par scripts/avec-tunnel-staging.sh (réutilisé s'il tourne déjà).
# REDIS_URL reste volontairement local (pas de tunnel redis) : la queue de jobs Bull ne doit
# jamais pointer sur le redis staging, sous peine de concurrencer le worker staging réel.
ENVIRONMENT=staging
DATABASE_URL=${DATABASE_URL_STAGING}
REDIS_URL=${REDIS_URL_LOCAL}
OIDC_ISSUER_URL=${OIDC_ISSUER_URL_STAGING}
